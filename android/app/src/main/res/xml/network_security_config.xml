<?xml version="1.0" encoding="utf-8"?>
<network-security-config>

    <!--
        Configuración base para todas las conexiones:
        - HTTP plano prohibido
        - Solo se confía en CAs instaladas por el fabricante del SO,
          no en certificados que el usuario instale manualmente.
          Esto bloquea proxies como Burp Suite o Charles en dispositivos
          con Android 5-6 donde por defecto sí se confiaba en CAs de usuario.
    -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!--
        Excepción para desarrollo local.
        Metro bundler y la API en 192.168.3.10:8000 usan HTTP (IP de la máquina de desarrollo en la red local).
        127.0.0.1 / localhost para pruebas en emulador en la misma máquina.
        10.0.2.2 es el alias del host en el emulador de Android Studio.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">192.168.3.10</domain>
    </domain-config>

</network-security-config>
